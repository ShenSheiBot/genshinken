# 西方負典内容处理代理规则

本文件适用于整个仓库。任何自动化工具、AI 代理和人工编辑在处理原稿、译稿、OCR 稿及 Outline 文档时都必须遵守。

## 1. “清洗”永远不是“改写”

默认清洗模式是**机械保真清洗**。其目标是在不改变表达的前提下修正载体和格式，绝不负责“让文章更像文章”。原文字词、自然段、顺序、论证、语气、修辞、重复、犹疑和节奏均受保护。

允许的动作仅限：

- 统一换行、文件末尾换行、文件名和 slug；
- 直引号转方向性弯引号；
- 脚注、链接、图片路径和 Markdown 记号规范化；
- 有原始页图、录音或可信底本逐项支持的 OCR 错字修正；
- 用户明确逐项指定的字词修订。

除非用户明确要求另作改写稿，否则禁止：

- 摘要、缩写、扩写、释义或同义改写；
- 删除、合并、拆分或重排自然段；
- 补写过渡句、结论、背景和论证；
- 把口语、讽刺、重复、犹疑、激烈措辞或机器翻译感改成中性论文体；
- 以“病句”“重复”“不流畅”为由重写整句；
- 用生成文本覆盖 Outline 或仓库中的原始文档。

## 2. 零差异保真门禁

- 归一化换行、BOM 和文件末尾换行后，扣除清单声明的机械转换及逐项授权修订，正文字符保留率必须为 **100%**。
- 自然段数量、边界和顺序必须完全一致；未授权新增、删除、替换、合并、拆分或重排必须为 **0**。
- 不得用相似度、摘要比对、模型语义判断或人工目测替代严格比较。
- 任一字符或段落不一致即失败；失败时不得提交、推送、部署或继续更新 Outline。

## 3. 源文快照先于任何修改

1. 修改前先读取并保存完整源文；不得只保存节选或搜索结果。
2. 需进入博客的源文保存在 `editorial-sources/`，文件一经登记即视为不可变快照。
3. 每个受保护派生稿必须登记到 `editorial-sources/preservation-manifest.json`，包含源文件 SHA-256、来源 URL、合并方式和唯一允许的机械转换。
4. 不得原地修改已登记快照。确需订正时，新增带版本标识的源文件，并在得到用户明确授权后更新清单。

## 4. 所有非自动修订必须逐项授权

OCR 订正和用户指定字词修订必须逐项登记到文档的 `authorizedChanges`，至少包含：

- 稳定 id 与类型（`ocr-correction` 或 `user-specified-wording`）；
- 精确 `find` 和 `replacement`；
- 理由、证据、授权人、授权日期。

每项 `find` 必须唯一匹配且不得跨换行。缺失、歧义或段落级改动直接失败。自动机械转换也必须在清单声明并由确定性脚本实现；脚本未实现的转换不得由模型临时执行。

## 5. 派生稿必须由确定性程序生成

- 使用 `npm run sync:preserved` 从源文快照重建受保护博客正文；程序必须先验证全部清单和源哈希，再写入任何文件。
- 使用 `npm run verify:preservation` 检查字符、段落、顺序、允许转换和授权修订。
- `npm run verify:snapshot-history` 禁止修改、删除或改名既有快照，只允许新增版本文件。
- `npm run check` 已包含快照历史与正文保真门禁；任一门禁失败时不得提交、推送、部署或更新 Outline。
- 禁止手工“顺便润色”程序生成的正文。需要变更时先新增经授权的源版本或授权修订，再重新生成。

临时比较两个文件时使用：

```bash
node scripts/verify-source-preservation.mjs --before path/to/before.md --after path/to/after.md
```

若唯一差异是成对 ASCII 双引号转弯引号，可额外传入 `--smart-double-quotes`。

## 6. Outline 更新规则

- 优先使用版本历史恢复或精确 `patch`，不得把 AI 生成的全文作为“清洗结果”整体覆盖。
- 更新前必须保存文档 ID、修订号和完整正文；更新后立即回读全文，与预期结果做严格比较。
- 保真比较失败时立即停止后续文档和博客操作，并使用版本历史还原。
- 多篇合并只发生在博客派生稿中；Outline 原文默认保持独立、原样和可恢复。

## 7. 真正的改写必须另立文档

如果用户明确要求摘要、改写、重组或重写：

- 新建独立草稿、独立文件或新 slug；
- 标明“改写稿／摘要稿”及其来源；
- 原稿不得被覆盖、改名顶替或静默删除；
- 未经用户确认，不得把改写稿发布为原稿的更新版本。
