# cv22801774 编辑证据

## 来源、日期与边界

- 原始归档：`.local-archive/bilibili-raw/source-archive/articles/cv22801774.json`；公开证据快照：`editorial-sources/roof-archive/cv22801774.json`。两者逐字节 SHA-256 均为 `90409e6c1c06896cabf0f31f39903d3ba0203c048dc774edb5482a8e9939b69d`；快照内内容完整性值为 `9a25ebf9851a377e570fd01dd84878fa732dd403d4c0dae4d00341a944893936`。
- B 站专栏 `cv22801774`，对应 opus `779875012135878672`，规范地址 `https://www.bilibili.com/opus/779875012135878672`。目录发布时间戳 `1680417201` 换算为东八区 `2023-04-02 14:33:21`，公开日期记为 `2023-04-02`。
- 原页明记《小角色的时代》，幻冬舍官方书目确认《リトル・ピープルの時代》于 2011-07-27 发售，512 页，ISBN `9784344020245`。本页实际收录第二章第15、16节，故以独立 `bookSection` 边界发布。
- 全档可证的五页同书选译顺序为：`cv20028261` ①（第1、2节）、`cv20166209` ②（第4节）、`cv22116444` ③（第7节）、`cv22801774` ④（第15、16节）、`cv23048003` 完结页（从第17节开始）。篇号、同书署名、章节号与终篇声明直接建立关系。
- 这不是连续全译：第3、5－6、8－14节未在五页中刊出；本页译者又明言与《零零年代的想象力》重复的早期假面骑士论述不重复翻译，第15节内还有“跳过一些剧情复读”的明示省略。因此不补造缺节，不在本任务改共享 book；五页精洗后由主线统一建立 `paused` 选译容器。

## 署名、许可与分类

- 页首明确“作者：宇野常宽”、“翻译：柴来人”，没有校对署名。两人均已在贡献者登记簿中，本篇无 contributor 缺口。
- 原页声明译文基于 `CC BY-NC-SA 4.0` 发布，许可进入 front matter 与 citation。“仅供个人学习”与本篇特有的跳译说明保留在页首来源注；招募、投稿邮箱与自我调侃式删文 CTA 是平台时期通用宣传，不进入公开正文。
- 栏目为“译”，主题分类为“影视”。标签复用同书已建立的宇野常宽、小角色的时代、特摄、假面骑士，并用全文主轴“角色复数性”连入既有角色研究；不把剑崎、桃塔罗斯、凯等一次角色拆成低复用标签，也不为每部骑士作品同时建并行标签。

## 正文、注释与机械处理

- 完整读取 `rawPageState.detail.modules[].module_content.paragraphs` 的 19 个顶层单元：1 个署名／许可块、10 个多自然段文字单元、8 个图片单元。按源顺序恢复第15、16节、所有自然段、译注省略标记与全部图位，不以摘要替代正文。
- 源文只有一个注号 `[1]`，未附定义。公开稿转为 GFM 角标，并如实说明“原页未提供对应注释正文”；不外补宇野与东浩纪对谈的书目。
- 只做可复核的机械规范化：日文书名括号形式统一；源中用于并列译法的 `\\` 与汉字间 `/` 转为 `／`；等值关系转为 `＝`；译者方括号补语转为 `〔〕`；`30~50代`、`90年代末~00年代前半期` 的范围连接转为“至”。译文的口语、重复、括号补充与“试行错误”等用词均保留。

## 图片与资产

- 快照 `content.imageReferences` 共 24 项。与结构化正文的 8 个图片单元逐项对照后，确认为 8 张唯一正文 JPEG；其余是列表、账号／会员／应用界面素材，或正文图的 AVIF/WebP 响应式重复。
- 8 张原图全部本地化，分辨率范围 500×578 至 1728×1080，单图 58,683–290,684 bytes。逐图可视检查确认为《剑》《响鬼》《Kabuto》《电王》《555》的角色、剧照或影像制品图，均与邻近论述直接相关，不是平台装饰。
- 等共享 manifest 并发写入稳定后，在主线确认的 2,375 项 union 基线上定向追加本篇 8 项至 2,383；`jq` 语法通过，8 项路径、bytes、SHA-256 与 MIME 均与本地文件一致。未运行默认 manifest 重建。

## 公开稿

- `source/_posts/uno-tokusatsu-history-4-showa-heroes-transformation.md`
- `public/attachments/roof-archive/cv22801774/` 下 8 张正文图。

## 门禁

- `npm run validate:content`：通过（253 篇文稿、262 位贡献者、21 本书、5 个专题、0 项非阻塞警告）。
- `npm run verify:typography`、`npm run verify:citations`、`npm run validate:media-html`、`npm run verify:han-script`、`npm run verify:snapshot-history`：通过。
- `npm run audit:tags`：通过，无规范化冲突。`npm run audit:roof-archive` 已把本篇识别为具有精确快照、编辑证据与公开引用的完成稿。
- `cmp -s` 确认公开证据 JSON 与原始 JSON 逐字节一致；逐项复核 19 个顶层结构化单元、其中的全部自然段、两个节标题、一个缺定义注号与 8 个图位，顺序与跳译边界均与源一致。
- 8 个本地图片引用均解析到非空文件，manifest 中恰有 8 项 `cv22801774` 记录，且 bytes／SHA-256 逐项相符；公开稿无远程图片热链。本篇相关 `git diff --check` 通过。
- 依任务边界未创建或修改共享 book/topic。本轮不运行共享 build/dev，留给主线在五页合并后做整批构建与渲染验收。
- Canonical 状态：五页齐备后，本篇独立正文已并入 `uno-tokusatsu-history-1-hero-circuit-monsters-postwar.md#sections-15-16`，作为共享 `paused` 书目 `uno-tokusatsu-history-little-people-era` 的选译④；原 standalone 路由已移除。第8－14节未译和第15节内部省略仍在共享书目说明与正文中明示；原快照与证据文件继续保留。
