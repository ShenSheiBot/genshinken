# 西方負典前端产品与验收规格

状态：**现行**
适用范围：全站导航、首页、文库、正文与多媒体、书籍、专题、关于页及发布发现面。
最后更新：2026-07-18

本文记录已经确认的产品决策和可验证的验收条件。实现细节可以调整，但不得在没有同步更新本文和回归检查的情况下改变这些行为。`docs/toc-rail-design.md` 以及移动端两端对齐研究属于历史探索，不是现行验收依据。

## 1. 全站品牌与语言

- 站名固定为「西方負典」，导航、Hero、页脚和结构化数据不得使用历史误称。禁用名称由发布回归脚本以 Unicode 码位维护，避免字面量重新进入仓库。
- 首页刊头使用「東流不溢，孰知其故」；不得出现用于描述内部版式方案的名称。
- 全站一级导航固定按「专题」「连载」「文库」「关于」排序，分别进入 `/topics`、`/books`、`/library`、`/about`。论、评、译介与多媒体是文库内部栏目，不再占据全站一级导航；文库内部也不重复设置连载入口。导航项只以留白区隔，所有断点均不使用竖向分隔线。
- 面向读者的界面采用**中文单语**。已有中文的英文对照全部删除；只有英文的操作标签应汉化。URL 参数、front matter 枚举值、程序标识符及作品原名不属于界面双语对照。
- 全站页脚统一只保留居中的站点 Logo 与其右侧使用 FOT 品牌字体的「西方負典」，不再重复站外链接、邮箱、投稿入口或授权信息。格式为 `YYYY.MM.DD UTC HH:MM:SS` 的构建时间只在 About 页的品牌组下方显示；Logo 资源不得因首页或正文改版丢失。
- 华文宋体、华文仿宋和华文楷体均以同源 WOFF2 语料子集自托管，避免依赖 iOS/iPadOS 不存在或网页不可调用的中文衬线系统字体。正文标题使用华文中宋，并以自托管华文宋体可靠回退；目录、注释、文献、译介标记等界面文字继续使用 CJK 无衬线界面字体，不使用华文楷体。华文楷体目前只作为已托管的内容字体变量预留，不进入现有界面。
- 所有主要交互必须有键盘可达的真实链接或按钮、可见焦点状态和中文可访问名称。模态弹层支持 `Escape` 关闭、焦点约束和关闭后的焦点恢复。
- 公开署名角色只有作者与译者，对应 `author / translator` 和「作／译」。角色方块（`cmark`）不参与超链接；只有贡献者姓名可点击，并进入该人在文库中的全部署名内容。

## 2. 首页

### 2.1 内容结构与权重

首页展示四类内容。可见顺序和编辑权重固定为：

1. `essay` → 论
2. `review` → 评
3. `translation` → 译介
4. `multimedia` → 多媒体

权重表示首屏视觉面积、排序优先级和阅读引导强度依次递减，并不要求四类数量相等。四类名称作为推荐卡页眉信息出现；按栏目筛选由文库面板承担，不恢复首页旧的四栏目导航。

### 2.2 主展示区

- 主展示区共展示 **10 个条目**，并形成四个清晰的栏目视觉带；有内容的每个栏目至少保留一个入口。
- 卡片应密铺、边界整齐，不产生无意义的大块空白或看似缺卡的孔洞。
- 每个栏目的首个推荐条目可以获得更强的视觉层级；多媒体卡片仍然进入统一编辑序列，但链接到 `/media/<slug>`。
- 推荐位的概览语不得在数据层预先截断；没有手写 `excerpt` 时，自动摘要也必须提供完整首段。为了维持推荐墙的密铺节奏，除首张评类推荐外，其余卡片可以在视觉层紧凑截断；首张评类推荐的概览必须完整呈现，不得被卡片裁切。
- 每张推荐卡的页眉固定为「栏目 · 主题分类」，例如「论 · 历史」「评 · 游戏评论」；页眉不再显示 `#03` 一类栏目或类别编号。
- 译介推荐只显示作者署名，不显示译者或其他贡献者，也不再发明「原作者／译者」两层展示字段。完整的作者、译者署名仍在正文、书籍页与文库筛选中保留。
- 推荐墙沿用用户确认的 P3 海报构图，但所有卡片的背景装饰层不再放置「论／评／译／媒」等栏目汉字。栏目内编号、斜向轴线、横向网格与圆形装饰继续保留；多媒体继续保留黑底反白、同心圆、红色轴线、红黑短标、巨大数字页码和平台条。移动端可以降低固定最小高度，但不得以隐藏标题、署名、摘要、平台条或页脚换取压缩。几何元素保持低对比度，标题、署名与摘要始终优先可读；不得额外加入被否决的 A／B 方案装饰或在移动端恢复旧栏目导航。

### 2.3 最新更新

- 主展示区之后展示最新 **6 篇非多媒体文章**；桌面为两行、每行三篇，中等宽度可变为两列，窄屏按单列阅读。
- 最新更新的概览语允许在视觉层省略，但完整标题、栏目、日期和文稿编号必须可见。
- 最新更新卡同样展示作者信息：角色 cmark 为普通文本，只有姓名链接到 `/library?contributor=<id>`。译介卡沿用首页推荐规则，只展示原作者，不显示译者；区块标题不再使用无语义的装饰编号。
- 文稿编号使用全站实际发表顺序：最早发布为 `1`，以后递增；不得使用卡片序号或固定的 `01`。
- 区块提供「查看全部文章」并链接 `/library`，不显示旧的文章数量提示。

### 2.4 首页验收

- 页面恰有 10 个带 `data-section` 的主展示条目；四个合法栏目值都应出现在推荐墙中，一级导航仍只显示专题、连载、文库、关于。
- 每张卡片页眉满足「栏目 · 主题分类」且不含类别编号；译介卡的贡献者链接只包含作者。
- 桌面端与移动端均需目视确认推荐墙沿用 P3 海报系统、多媒体的既有海报元素完整保留、标题／署名可读且没有恢复被否决的 A／B 方案；这些几何关系不以 CSS Modules 生成类名作为发布脚本断言。
- 最新更新最多 6 篇且不包含 `multimedia`；编号与内容层计算结果一致。
- 首张评类推荐摘要完整呈现；其余推荐与最新更新摘要可以为紧凑布局截断，但源数据不得预先截短。
- Hero 文案、页脚 Logo、中文单语和禁用词检查通过。

### 2.5 动效

- 全站采用已经选定的校样落版节奏：首屏标题与版线按排版顺序出现，列表及内容区在进入视口时使用低幅纵向揭示；正文阅读流本身不做动画。
- 动效是正式界面的一部分，不提供面向读者的方案切换器、URL 参数或本地存储选项。
- Hero 主标题的裁切揭示必须覆盖紧凑行盒以外的完整 CJK 字形；动画结束前后不得补笔、跳变或裁字。
- 首页卡片外壳及其底色从首帧起必须保持可见，只允许卡片内容揭示；刷新时不得暴露用于绘制 1px 分隔线的深色网格底板。
- `prefers-reduced-motion: reduce` 下取消位移、透明度与裁切揭示，内容直接可见。

## 3. 文库

### 3.1 URL 状态

- 唯一 canonical 路由为 `/library`，当前不提供文字搜索框。旧 `/search` 必须以 `308` 永久跳转，并原样保留合法或未知查询参数。
- 支持至多各一个 `section`、`category`、`tag`、`contributor`、`role` 查询参数；五个参数可组合、可分享、刷新后状态不丢失。
- `contributor` 使用 `lib/contributors.ts` 中稳定的 ASCII id；`role` 只能是 `author / translator`。姓名或显示文字不得充当 URL 身份。
- 取消某一筛选时只移除对应参数，保留其余有效条件。手工输入的无效筛选值以 `307` 重定向到只保留其余有效条件的规范 URL，而不是悄然显示无关的全量结果。
- 手工构造的互斥 URL 可以显示空状态；正常界面路径不得把用户带入零结果状态。

### 3.2 SKU 式组合筛选

- 筛选分为「栏目」「主题分类」「标签」「贡献者」「署名位置」五组，使用可折叠的 `<details>`；桌面默认展开，移动端默认全部收起，读者可独立折叠每一组。
- 每个选项的计数在**保留另外四组当前条件、替换本组条件**后计算。
- 计数为零且当前未选中的选项必须呈灰态并使用原生 `disabled`，不可点击、不可获得普通交互状态。
- 当前选项始终允许取消；「全部」始终允许选择。选项必须显示当前组合下的结果数。
- 筛选区保持紧凑并使用站点统一的自定义滚动条，不出现浏览器默认滚动条样式。

### 3.3 结果与验收

- 结果区标题为「内容索引」，界面中不出现中英双语列名或单独的「打开」列。
- 移动端 Hero 将当前数量／内容总数与「内容索引」排在同一行；当前数量使用次于标题的字号层级，且两者在窄屏不得换行或互相遮挡。
- 条目按内容层既定顺序展示，链接按栏目进入 `/posts/<slug>` 或 `/media/<slug>`。
- 文章署名和书籍署名中的姓名链接到 `/library?contributor=<id>`，默认展示该人在全部署名位置的内容；角色细分由文库中的「署名位置」面板完成。姓名链接默认不得自行附加 `role`。
- 文库只索引文章和多媒体，不提供「文章与媒体／书籍与连载」双入口；书籍清单及连续阅读功能由一级导航中的 `/books` 独立承担。
- 首页推荐卡页眉展示正确的栏目和主题；文库筛选面板生成的任一正常组合 URL 在分享后，活动项、计数和结果保持一致。
- 自动检查应覆盖全部真实栏目、主题分类、标签、贡献者和署名位置，确认正常界面生成的任一链接均至少得到一条结果。

## 4. 正文阅读页（案卷方案）

### 4.1 通用布局

- 正文页采用案卷式阅读界面，桌面与移动端按各自信息密度设计，不只是等比例缩小。
- 文章档案只保留实际存在的署名信息；原文题名、原刊和日期缺失时整行不渲染，不显示占位文案或资料声明。
- 当前滚动子标题与导航左侧主标题使用一致的字号、字体和垂直对齐。
- 目录、注释和文献使用站点自定义滚动条。侧栏或弹层不得与页脚重叠，滚到文章底部时仍可完整访问。
- 署名按作者、译者顺序展开为独立贡献者记录。角色方块不在链接内；姓名链接到 `/library?contributor=<id>`。JSON-LD 中相应 Person 也使用同一稳定筛选 URL。
- 正文封面的栏目文字链接到 `/library?section=<section>`，栏目内编号保持非链接；每个 `#标签` 分别链接到 `/library?tag=<编码后的标签>`。这些入口只建立被点击的单项筛选，不隐式叠加文章的其他栏目、主题或标签条件，也不再使用旧 `/search` 路径。
- 窄桌面端与移动端的文章目录抽屉只保留阅读进度、视觉行跳转、文章目录和返回篇首，不得重复页眉已经提供的「专题／连载／文库／关于」全站导航。
- 移动封面不重复设置「本文信息」折叠栏。发布日期和预计阅读时长与「返回首页」处于同一行并靠右；档案号／栏目与返回行之间不设横贯分隔线，并以紧凑留白衔接。桌面封面与正文之间、正文完与相关推荐之间同样不使用横贯版心或视口的分隔线。
- 桌面正文页眉与主页共享同一 1280px 版心契约：品牌锁组对齐版心左缘，专题、连载、文库、关于四项紧随品牌靠左排列，底部分隔线与阅读进度只横跨版心而不贯穿视口。移动端保留章节按钮与阅读工具的专用重排，底线对齐其 16px 内容边距。

### 4.2 视觉行进度

- 进度以浏览器实际排版后的**视觉行**为单位，而不是 Markdown 源文件行号。
- 界面同时显示当前行、总行数和进度条，并提供输入具体行数后跳转的控件；非法值不可提交，越界值应被拒绝或限制到合法范围。
- 字体完成加载、容器尺寸变化、图片加载和正文布局变化后必须重新测量；测量中状态应稳定，不得把临时计算样式固化为最终过大字距。
- 跳转后目标视觉行应避开固定导航，并尊重 `prefers-reduced-motion`。

### 4.3 目录

- 目录位于左侧文章档案下方；一级结构常见可见，二级及更深标题必须能够逐支折叠和展开。
- 当前标题使用黑色或主题的 `--ink`，而不是灰色；当前项和已展开状态必须可由非颜色线索识别。
- 点击目录项跳转到对应正文标题，并使该标题避开固定导航。

### 4.4 注释与文献

- 宽桌面右侧仅渲染文章实际具有的注释或文献；两者都有时上下分区、独立滚动，只有一类时占满侧栏高度，两者都没有时整栏不显示。未选中条目使用主题的柔和灰色，选中条目以红色标签和黑色或 `--ink` 正文明确区分。
- 移动端不重复渲染同一条注释。由注释中的文献引用进入文献时，应在同一个弹层内切换内容，并提供清楚的返回路径；不得先关闭弹层再在页面下方滚动到另一组内容。
- 返回操作需要保留跨引用历史；关闭后恢复到触发脚注、文献引用或工具按钮。

### 4.5 断点与入口

- `>=1360px`：使用完整桌面案卷。左侧为署名、视觉行与目录；右侧按文章实际具有的引用类型显示注释、文献或双区。
- `768–1359px`：可以收起左右侧栏，但工具区必须提供明确的「目录」入口；打开后仍可访问当前行/总行数、行号输入跳转和完整可折叠目录。不得出现功能真空区。
- `<=767px`：使用移动工具栏和底部弹层访问目录、注释、文献与阅读设置；保持触控命中区和安全区域。

### 4.6 字体与移动排版

- 阅读设置只显示「衬线」「无衬线」和字号等读者可理解的选项，不展示具体字体方案清单。
- 衬线正文：中文正文以本站自托管华文宋体为首选，西文/数字以 Times New Roman 链为首选；引文使用本站自托管华文仿宋与 Garamond 链。CJK 引号和间隔号必须由合适的中文字体承载，不得被西文字体带走。
- 无衬线正文：中文正文使用等线、苹方等 CJK 无衬线链，西文/数字使用 Helvetica Neue 等西文无衬线链；引文也采用相应的 CJK/西文无衬线组合。
- 标题在两种正文主题下均使用华文中宋链；目录、注释、文献和设置等 UI 使用 CJK 无衬线界面字体。切换主题时西文、数字和标点必须一起切换；背景不得出现模糊竖线。
- `<=767px` 的正文使用 `text-align: start` 与 `letter-spacing: 0`，依靠浏览器原生中文断行，不强制两端齐行。版心左右内边距固定为**左 24px、右 16px**（逻辑方向环境下保持同等视觉意图）。
- 旧研究文档中关于强制 `justify`、实验性 em 字距和自定义整段断行的内容仅供研究，不构成现行标准。

### 4.7 正文验收

- 至少在 375px、768px、1024px、1280px、1360px 和宽桌面检查目录、行号、注释、文献与页脚。
- 使用 Firefox 与 Safari/WebKit 检查移动断行；最终样式必须保持起始对齐和零额外字距，不以两浏览器恰好在同一字符换行作为验收条件。
- 用键盘检查工具按钮、目录树、跨注释/文献引用、弹层焦点循环、`Escape` 和焦点恢复。

## 5. 多媒体详情页与内容模型

- 博客**不播放或嵌入任何多媒体**。多媒体条目拥有 `/media/<slug>` 站内详情页，只展示标题、简介、封面/静态资料、站外平台链接和关联站内文稿。
- 站外链接可以指向 YouTube、哔哩哔哩、Apple Podcast 等平台；必须清楚标示平台并安全地在新窗口打开。
- 页面产物不得包含 `<iframe>`、`<video>`、`<audio>`、`<object>` 或 `<embed>`。这一限制既适用于组件，也适用于 Markdown 正文中的原生 HTML。
- `related_posts` 是仅允许 `section: multimedia` 使用的 front matter 列表。每个值必须是已存在、已发布且非多媒体的显式文章 slug，不得重复或指向自身；详情页按声明顺序展示零篇或多篇关联文稿。
- `featured_order` 是所有栏目均可使用的可选有限数值，默认 `0`，数值越大则在同栏目首页排序越靠前；数值相同时回退到日期、`sort_order` 和 slug 的既有稳定顺序。不得再按某个 slug 写死排序特例。
- 旧 `/posts/<multimedia-slug>` 必须永久重定向到对应 `/media/<slug>`。

## 6. 书籍与连载

- `/books` 是书目索引；`/books/<slug>` 是一本书的状态、说明、章节入口和阅读选择页。书籍清单位于 `source/_books/*.json`，与承载连续正文的 `documentSlug` 分离。
- 正文仍是一份可连续滚动、全文查找的文章，不按章节拆成互相隔离的内容页。`/books/<slug>/chapters/<chapter-id>` 是稳定入口，并以 `308` 跳到连续正文中的稳定标题锚点；这些跳转入口不作为 sitemap canonical 项。
- 书籍页始终提供「从头阅读」「阅读最新章节」两个明确入口。读者每次进入自行选择起点；不自动恢复、不使用弹窗，也不记录本地阅读位置。
- `latestChapterId` 必须指向现有章节；章节 id、number 与 anchor 在书内唯一。书籍 `updatedAt` 与承载正文的 `updated/date` 同步更新。
- 书籍页作者／译者姓名使用贡献者稳定 id 链接文库。Book JSON-LD 及 Chapter URL 必须为绝对 URL，章节 `position` 从 1 开始，包含 publisher、发布日期和修订日期。
- 书籍清单可分别提供 `originalBibtex / translationBibtex / pdfUrl / epubUrl`。原书与译本书目信息是两个独立复制位：有对应 BibTeX 才显示紧凑的复制控件，并提供成功／失败反馈；缺失资料不得用另一版本或虚构记录代替。
- PDF 与 EPUB 下载彼此可选，只在对应 URL 实际存在时显示真实下载链接；不得渲染禁用按钮、空链接或占位地址。站内文件使用根相对 URL，站外文件只接受 HTTP(S)。
- `/books` 与 `/books/<slug>` 应以清楚而紧凑的书目层级、连载状态和阅读路径为主；聚合页首屏应尽快露出首条书目，详情首屏应尽快进入两个阅读入口，不用超大留白或装饰遮挡书名、署名、章节与资源操作。聚合页主体与页脚之间不得使用横贯视口的分隔线，书目内部的版心分隔线保留。

## 7. 专题

- `/topics` 展示编辑部人工策展的主题集合；`/topics/<slug>` 展示导语、「从这里开始」和有序内容分组。专题不是标签搜索结果，也不由算法自动生成。
- 专题源位于 `source/_topics/*.md`。它包含 `title / subtitle / summary / status / published / updated`、可选 `curators`、正文导语和 `groups`；数组顺序就是公开编排顺序。
- 条目引用类型只能是 `post / media / book`，以稳定 slug 指向已发布实体，并可附 `editorialNote`。首组首项是「从这里开始」入口；重复、缺失、草稿或类型错配引用必须阻止发布。
- 专题详情使用 CollectionPage JSON-LD，并按编辑顺序输出绝对的 `hasPart` URL。专题索引与详情均进入 sitemap；专题不作为 RSS 内容条目重复发布。
- 专题索引与详情的视觉层级必须紧凑地区分专题状态／摘要、导语、分组和条目；索引首屏应露出首条专题记录，详情从浅色标题区直接进入导语。首组首项以内联「从这里开始」标识呈现，不得在页面中重复渲染同一条目。装饰不得穿过标题或编者按，也不得削弱人工阅读顺序。

## 8. 关于页与联系入口

- `/about` 只保留联系入口，以「联系我们」为主标题，并使用 `info@un-canon.com` 与 `editor@un-canon.com` 的既定职责分工；站外主页在同一区域列出。
- 发表过文章或翻译不等于编辑部成员。贡献者资料不从一次署名自动推断为团队身份。
- About 使用独立 canonical 与 OpenGraph，不使用首页的通用分享标题；其页脚沿用全站居中的品牌组，并作为唯一显示「最新修改」构建时间的位置，不再设置单独的左对齐署名或重复联系信息。

## 9. 搜索发现与订阅

- 所有可索引页面必须输出与自身一致的 canonical；文章、多媒体、书籍与专题详情输出匹配实体的 OpenGraph 和 JSON-LD。结构化数据中的站内 URL 使用绝对地址。
- sitemap 包含首页、专题／文库／书籍／关于聚合页、专题详情、书籍详情以及所有文章和多媒体 canonical；不包含 `/search`、章节跳转入口或带筛选查询参数的文库 URL。
- 聚合页 `lastmod` 取对应内容源的最近真实更新时间，不使用构建时间。About 没有可靠内容日期时省略 `lastmod`。
- RSS 保持“已发布文章与多媒体正文”这一内容流；书籍正文已由其 `documentSlug` 文章进入 feed，书籍落地页和专题策展页不再生成重复条目。RSS 内部资源 URL 必须绝对化。
- 文章、书籍或专题源变更推送 `main` 后，IndexNow 同时提交实体 URL 和相应聚合页；Google 继续依靠 sitemap。

## 10. 自动门禁与变更规则

- `npm run validate:content` 校验贡献者登记与署名、文章 front matter、书籍／章节清单、专题编排、跨实体引用、日期一致性和唯一性。
- `npm run validate:media-html` 以恶意样本校验多媒体 HTML 允许列表、主动内容和危险属性过滤。
- `npm run typecheck`、`npm run lint`、`npm run build` 必须非交互执行并以非零状态阻止合并。
- `npm run verify:release -- <base-url>` 对已经启动或线上站点执行导航、文库筛选、贡献者链接、正文、多媒体、书籍、专题、About、重定向、canonical、OpenGraph、JSON-LD、sitemap 和 RSS 回归。`verify:editorial` 保留为兼容别名。
- `npm run check` 是本地静态交付门禁；GitHub Actions 对每次影响站点的推送和拉取请求至少运行依赖安装、内容校验、TypeScript、ESLint 和生产构建。
- 任何改变本文规范的产品决策，应在同一变更中更新本文、相关交付文档和对应自动断言。
